# MemeVault direct dependencies
# Install with: .venv/bin/pip install -r requirements.txt

# UI
PySide6==6.11.1

# Image processing & perceptual hashing
Pillow==12.2.0
ImageHash==4.3.2

# AI / VLM
torch==2.11.0
torchvision==0.26.0
transformers==5.8.1
accelerate==1.13.0
bitsandbytes==0.49.2
qwen-vl-utils==0.0.14

# Optional: OCR (Tesseract wrapper — requires system Tesseract installation)
# pytesseract
